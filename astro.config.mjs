// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Set this when we go live so canonical URLs + sitemap are correct:
  // site: 'https://laurenk.im',
});
